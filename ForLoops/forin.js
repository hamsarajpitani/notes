//!! for in allows u to loop through properties of object
//* enumeraton means "countble" object ke properties ko lookout karna

const goals = {
    first : "smarthome",
    second : 'food truck',
    third : 'giveback'
}

console.log(goals);

for(abc in goals){
    console.log(abc);
}
